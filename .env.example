# Database Configuration (for production deployments like Railway)
JDBC_DATABASE_URL=jdbc:postgresql://localhost:5432/bookstore_db
JDBC_DATABASE_USERNAME=your_db_username
JDBC_DATABASE_PASSWORD=your_db_password

# Optional: Server Port (Railway provides PORT automatically)
PORT=8080